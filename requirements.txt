# Core ML Frameworks
tensorflow==2.19.0      # Strict pin is okay here for model stability
numpy==2.0.2            # Strict pin required for ABI compatibility
pandas==2.2.2
scikit-learn==1.6.1

# Image Processing
# FIX: Use '>=' to allow the cloud environment to find the latest STABLE release
opencv-python-headless>=4.10.0  
Pillow>=10.0.0

# Application Server
streamlit>=1.32.0